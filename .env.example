# Optional: enable Claude-powered chat in the Schedule Assistant.
# Deploy /api/assistant.js (needs ANTHROPIC_API_KEY on the server), then set:
# VITE_ASSISTANT_ENDPOINT=/api/assistant

# Supabase (public values — also stored in src/data/supabase-config.js as defaults)
# VITE_SUPABASE_URL=https://xxxx.supabase.co
# VITE_SUPABASE_ANON_KEY=sb_publishable_...
